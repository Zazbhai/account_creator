# Backend Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=6333
FLASK_DEBUG=False
SECRET_KEY=your-secret-key-change-this-in-production

# Frontend URL for CORS
# Replace yourdomain.com with your actual domain
FRONTEND_URL=https://yourdomain.com

# CORS Origins (comma-separated)
# Add both your domain and localhost for development
CORS_ORIGINS=https://yourdomain.com,https://api.yourdomain.com,http://localhost:7333,http://localhost:6333

# Frontend Configuration (Vite)
VITE_FRONTEND_PORT=7333

# Backend URL for frontend to connect to
# For production with domain
VITE_BACKEND_URL=https://api.yourdomain.com

# For localhost development, use:
# VITE_BACKEND_URL=http://localhost:6333

# Allowed hosts for Vite dev server (comma-separated)
VITE_ALLOWED_HOSTS=yourdomain.com,api.yourdomain.com

# Session Cookie Settings (set to True for HTTPS)
SESSION_COOKIE_SECURE=True

DATABASE_URL=postgresql://neondb_owner:npg_2OJ0rvhmnCVE@ep-lingering-glade-ahxz3ciz-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
